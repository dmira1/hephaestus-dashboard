import styled from 'styled-components';

const PageNotFoundStyles = styled.section`
    text-align: center;
`;

const PageHeader = styled.h1`
    font-size: 36px;
`;

const PageImage = styled.img`
    margin-top: 3rem;
    margin-bottom: 1rem;
    width: 300px;
`;

export {PageNotFoundStyles, PageHeader, PageImage}